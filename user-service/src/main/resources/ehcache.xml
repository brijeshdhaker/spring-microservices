<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.ehcache.org/v3"
        xmlns:jsr107="http://www.ehcache.org/v3/jsr107"
        xsi:schemaLocation="
            http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd
            http://www.ehcache.org/v3/jsr107 http://www.ehcache.org/schema/ehcache-107-ext-3.0.xsd">

    <!-- Persistent cache directory -->
    <persistence directory="/tmp/cache" />

    <cache alias="userCache">
        <key-type>java.lang.Long</key-type>
        <value-type>sample.multimodule.dto.request.UserRequestDTO</value-type>
        <expiry>
            <ttl unit="seconds">60</ttl>
        </expiry>

        <listeners>
            <listener>
                <class>sample.multimodule.configs.cache.CacheEventLogger</class>
                <event-firing-mode>ASYNCHRONOUS</event-firing-mode>
                <event-ordering-mode>UNORDERED</event-ordering-mode>
                <events-to-fire-on>CREATED</events-to-fire-on>
                <events-to-fire-on>EVICTED</events-to-fire-on>
                <events-to-fire-on>EXPIRED</events-to-fire-on>
            </listener>
        </listeners>

        <resources>
            <!--
            For the on heap store we configure a capacity of 1,000 cache entries. This is the maximum number of entries before eviction starts.
            -->
            <heap unit="entries">200</heap>
            <!--
            For the off-heap store we configure a capacity of 10 MB.
            -->
            <offheap unit="MB">10</offheap>

            <disk persistent="true" unit="MB">20</disk>
        </resources>
    </cache>

</config>